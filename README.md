# Git-commands

```bash
git add --all
git commit -m "commit"
git push origin <branch | tag>
git pull
git status
git log
git log --oneline
git branch <new branch>
git checkout <branch>
git tag <new tag>
git tag <new tag> -m "commit"
```
